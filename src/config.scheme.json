{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "tailwindcss-plugin-icons configuration file",
  "type": "object",
  "required": ["iconSets"],
  "properties": {
    "iconSets": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "description": "The icon set name. Camel case names are converted to kebab case.",
        "properties": {
          "icons": {
            "type": "array",
            "items": { "type": "string" },
            "description": "The icon names to include. Leave out this property to include every icon in the icon set."
          },
          "location": {
            "type": "string",
            "format": "uri-reference",
            "description": "The location of the icon set in Iconify JSON format. Can be any HTTP location, module name, or local path."
          }
        }
      }
    },
    "prefix": {
      "type": "object",
      "description": "Prefix configuration for the Tailwind CSS utility classes.",
      "properties": {
        "mask": {
          "type": "string",
          "description": "The prefix for the mask-image icon utility.",
          "default": "mi"
        },
        "background": {
          "type": "string",
          "description": "The prefix for the background-image icon utility.",
          "default": "bi"
        }
      }
    }
  }
}
